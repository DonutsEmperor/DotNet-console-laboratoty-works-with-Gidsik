<UserControl x:Class="Lab8.Views.ExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab8.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


	<DockPanel LastChildFill="True">

		<ScrollViewer DockPanel.Dock="Top" Background="LightGreen">

			<TextBlock>
				<TextBlock.Text>
					<MultiBinding StringFormat="{}{0} - app_token">
						<Binding Path="ExhibittedToken"/>
					</MultiBinding>
				</TextBlock.Text>
			</TextBlock>

		</ScrollViewer>

		<Button DockPanel.Dock="Bottom" Background="BlanchedAlmond" Content="get the folders" Command="{Binding Post}"/>
		<Button DockPanel.Dock="Left" Background="Aquamarine" Content="Левая кнопка" />
		<Button DockPanel.Dock="Right" Background="DarkGreen" Content="Правая кнопка" />

		<ScrollViewer>
			<ListBox ItemsSource="{Binding FoldersList}" SelectedItem="{Binding SelectedFolder}">

				<ListBox.Resources>
					<Style TargetType="TextBlock">
						<Setter Property="MaxWidth" Value="100px"/>
						<Setter Property="FontSize" Value="18"/>
						<Setter Property="Margin" Value="20"/>
					</Style>
				</ListBox.Resources>

				<ListBox.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="{Binding Path=Id}" />
							<TextBlock Text="{Binding Path=Name}"/>
							<TextBlock Text="{Binding Path=Path}"/>
						</StackPanel>
					</DataTemplate>
				</ListBox.ItemTemplate>

			</ListBox>
		</ScrollViewer>

	</DockPanel>

</UserControl>
